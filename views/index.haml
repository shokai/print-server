- content_for :title do
  = @title

%div
  %h2 Print File
  %form{:method => :post, :action => "#{app_root}/file", :enctype => 'multipart/form-data'}
    %div
      File :
      %input{:type => :file, :name => 'file'}
      Printer :
      %select{:name => 'printer', :size => 1}
        - @printers.each do |pr|
          %option{:value => pr}= pr
    %div
      %input{:type => 'submit', :name => 'upload', :value => 'Print!!'}
